import React from 'react';
import { IThemeKey } from '../../interfaces';
import { GWGlobalConfig } from '../../vis/theme';
declare const _default: (({ themeConfig, themeKey }: {
    themeConfig?: GWGlobalConfig;
    themeKey?: IThemeKey;
}) => React.JSX.Element) & {
    displayName: string;
};
export default _default;
